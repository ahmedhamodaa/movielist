enum AppBarHeaders { home, aboutMe, projects, certificates, contact }

enum AppBarHeadersAxis { horizontal, vertical }

enum DeviceType {
  mobile,
  ipad,
  smallScreenLaptop,
  largeScreenDesktop,
  extraLargeTV,
}
